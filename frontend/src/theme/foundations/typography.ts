const typography = {
    fonts: {
        heading: "'Inter', sans-serif", // Font for headings (e.g., logo text)
        body: "'Inter', sans-serif",     // Font for body text
    },
};

export default typography;